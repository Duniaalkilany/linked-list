class Node {
    constructor(value,next){
        this.value=value;
        this.next=null;
    }
}
module.exports=Node
//we define a class Node having two properties: value and next. value holds the data
// of a node while next holds the pointer to the next node